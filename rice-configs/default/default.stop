# Stop compiled rice components
systemctl --user list-units --no-legend --plain --state=running \
  | awk '/app-Hyprland-ashell@/{print $1}' \
  | xargs -r systemctl --user stop;
systemctl --user stop swaync;
